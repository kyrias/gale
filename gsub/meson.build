gsub_sources = ['gsub.c', 'default.c']
gsub = executable('gsub',
                  gsub_sources,
                  include_directories: includes,
                  dependencies: [liboop, libgale],
                  install: true)
